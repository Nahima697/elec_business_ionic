import { VerifMethodPipe } from './verif-method.pipe';

describe('VerifMethodPipe', () => {
  it('create an instance', () => {
    const pipe = new VerifMethodPipe();
    expect(pipe).toBeTruthy();
  });
});
